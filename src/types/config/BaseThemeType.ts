export interface BaseThemeType {
    colors: {
      primary: string;
      secondary?: string;
      background?: string;
      text?: string;
      textHint?: string;
      toolbar?: string;
    };
  }